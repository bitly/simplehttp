
SimpleTokyo provides a light http interface to Tokyo Tyrant. It supports
the following commands.

get - key, jsonp (opt)
put - key, value, jsonp (opt)
del - key, jsonp (opt)
fwmatch - key, max (opt), length(opt), offset (opt), jsonp (opt)

# NOTE: 12/31/2009 by jehiah

    there is a known issue with libevent-1.4.9 about decoding url query parameters with '+' instead of '%20'.
    this is fixed in libevent-1.4.13. As url parameters with '+' will be stored with '+' instead of ' ' as of
    revision 66
